@model IEnumerable<LHL_lesson09.Models.Khoa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "LHLCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaKH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaKH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenKH)
        </td>
        <td>
            @Html.ActionLink("Edit", "LHLEdit", new { id = item.MaKH }) |
            @Html.ActionLink("Details", "LHLDetails", new { id = item.MaKH }) |
            @Html.ActionLink("Delete", "LHLDelete", new { id = item.MaKH })
        </td>
    </tr>
}

</table>
